# AI 订阅全球价格对比站 可行性调研

> 调研日期：2026-08-26
> 目标：参考 [appstoreprice.org](https://appstoreprice.org/zh)，做一个展示 ChatGPT / Claude / Gemini / Grok 在全球各地区订阅价格的站点
> 说明：本文的所有结论均来自当天实测，不是查资料推断。实测脚本跑在本仓库自己的 netcup VPS（德国，`159.195.18.74`）上，原始抓取数据和脚本保留在服务器 `/root/aiprice-probe/`（4 个产品 × 158 地区的完整快照，可直接当作项目的第一批种子数据）。价格是活数据，随时会变，文中的具体数字只代表 2026-08-26 当天。

## 1. 结论先行

**技术上完全可行，而且比预想的简单。** 核心数据源已经跑通并全量验证：

- Apple App Store 的商店页里有一段 `serialized-server-data` JSON，**公开、免鉴权**，包含该地区的应用内购买项目名称和本地货币格式化价格。
- 实测抓取 **4 个产品 × 158 个 App Store 地区 = 632 次请求，零抓取失败，零 429 限流**，合计约 40 分钟。
- 唯一的非 200 响应是 11 次 HTTP 404，全部是「该产品未在该地区上架」，与独立核对的上架数据完全吻合。
- 价格解析 + 汇率换算全链路跑通，主力订阅档位**全部解析成功**。

真正的难点不在"能不能拿到数据"，而在三件事：**价格字符串解析、SKU 语义缺失、以及合规定位**。下面逐条给结论。

### 可行性评级

| 环节 | 结论 | 证据 |
|---|---|---|
| 拿到各地区 App Store 订阅价 | ✅ **完全可行** | 158/158 地区抓取成功，见 §3.1 |
| 拿到各地区上架情况 | ✅ **完全可行** | iTunes Lookup API，见 §3.2 |
| 拿到汇率 | ✅ **完全可行** | 两个免费无密钥源，166 / 341 种货币，见 §3.5 |
| 价格字符串解析 | ⚠️ **可行但有坑** | 单个 SKU 就有 78 种格式模板；实测踩了两轮 bug，见 §5.1 |
| 区分月付 / 年付 | ❌ **拿不到** | Apple 不给周期字段，只能人工维护 SKU 表，见 §5.3 |
| 拿到全部订阅档位 | ⚠️ **主力档位全有，高价档有缺口** | ChatGPT/Claude 158 地区无损；Gemini Ultra 仅 87、Grok Heavy 仅 128，见 §5.2 |
| 拿到官网（Web）价格 | ❌ **基本不可行** | OpenAI 403、Anthropic 301，见 §3.4 |
| 拿到 Google Play 价格 | ❌ **只有价格区间** | 见 §3.3 |
| 在国内机器上抓 | ❌ **不可行** | Apple 对中国 IP 强制锁 `/cn/` 商店，见 §5.5 |
| 复用现有基建 | ✅ **零边际成本** | 现有 VPS + Cloudflare + MySQL 够用，见 §6 |

### 直接建议

1. **做，但把它定位成"价格透明度工具"，不是"跨区省钱教程"。** 竞品（geopriced、appstoreprice 等）靠 VPN 联盟营销和礼品卡变现，法律和账号风险全在这条线上。见 §8。**（已采纳，见 §10.1）**
2. **只用 App Store 商店页做唯一数据源**，别去碰 Web 官网价和 Google Play，投入产出比太差。
3. **必须人工维护一张 SKU 白名单**（哪个名字对应哪个套餐、月付还是年付、合理价格区间）。这是整个项目唯一需要持续人工投入的地方，大约 40 条记录。**ChatGPT 和 Claude 很规整，Gemini 明显最乱**——如果想先出成果，可以先上这两个。
4. **爬虫只能跑在海外机器上**，现有德国 VPS 正好合适。本地开发需要走代理。
5. 差异化不在"做一个价格表"——已经有一堆了——而在 **158 个地区的覆盖度（竞品 20–46 个）、每日自动更新、以及历史变价记录（竞品都没有）**。见 §7。

---

## 2. 参考对象拆解：appstoreprice.org 在做什么

以 ChatGPT 页面 `/zh/apps/6448311069` 为例，它的形态是：

- 一个 App 一个页面，按 **App Store 地区**列出该 App 的订阅价；
- 每行：国旗 + 地区名 + 本地货币原价 + 折算人民币 + 排名；
- 按折算价从低到高排序，顶部给出"最低比最高便宜 X%"；
- ChatGPT 页面展示 **32 个地区**（不是全部）；
- 页面没有标注数据来源和更新频率；
- 变现方式是站内**礼品卡商城**。

也就是说：它的数据形态就是"App Store 内购价 × 地区 × 汇率换算"。这正好是本次要验证的东西——而且下面会看到，它的 32 个地区其实**远没有把数据吃透**。

---

## 3. 数据源可行性：逐个实测

### 3.1 Apple App Store 商店页 — ✅ 主数据源

访问 `https://apps.apple.com/{地区码}/app/id{应用ID}`，返回的 HTML 里有一段：

```html
<script type="application/json" id="serialized-server-data">{...}</script>
```

这段 JSON 里包含一个 Annotation 节点，结构是：

```json
{
  "$kind": "Annotation",
  "title": "App 内购买",
  "items_V3": [
    {"$kind": "textPair", "leadingText": "ChatGPT Plus", "trailingText": "$19.99"},
    {"$kind": "textPair", "leadingText": "ChatGPT Go",   "trailingText": "$8.00"}
  ]
}
```

**这就是全部所需数据。** 无需登录、无需 API Key、无需 Apple Developer 账号。

四个目标 App 的 ID（用 iTunes Search API 核实过）：

| 产品 | App Store ID | Bundle ID | 开发者 |
|---|---|---|---|
| ChatGPT | `6448311069` | `com.openai.chat` | OpenAI OpCo, LLC |
| Claude | `6473753684` | `com.anthropic.claude` | Anthropic PBC |
| Google Gemini | `6477489729` | `com.google.gemini` | Google LLC |
| Grok AI | `6670324846` | `ai.x.GrokApp` | X Corp. |

**实测抓取性能**（德国 VPS，单线程，请求间隔 1.2 秒，四个产品全量跑完）：

| 产品 | 成功 | HTTP 404 | 429 限流 | 耗时 |
|---|---:|---:|---:|---:|
| ChatGPT | 158 | 0 | 0 | 629s |
| Claude | 155 | 3 | 0 | 609s |
| Gemini | 158 | 0 | 0 | 669s |
| Grok | 150 | 8 | 0 | 529s |
| **合计** | **621** | **11** | **0** | **约 40 分钟** |

**632 次请求零抓取失败、零限流。** 11 次 404 全部是「该产品未在该地区上架」——地区列表（`cd`/`mm`/`ye` 等）与 §3.2 独立核对的上架数据完全吻合，不是抓取问题。

四个 App 全量刷新约 **40 分钟 / 天**，完全够每日更新。

⚠️ 但**限流是真实存在的**：不加间隔连续请求，第 5 个请求就吃到 `HTTP 429`。节流是硬要求，不是优化项。

### 3.2 iTunes Lookup API — ✅ 辅助源（币种 + 上架情况）

`https://itunes.apple.com/lookup?id={应用ID}&country={地区码}` 返回该地区的 `currency` 字段和是否上架（`resultCount`）。

**它不返回内购价格**（App 本身免费，`price: 0`），所以不能替代 §3.1。但它提供两个关键的辅助数据：

**(a) 地区 → 币种映射。** 这是价格解析的基础事实，见 §5.1 为什么必须有它。实测 158 个地区共 **40 种货币**。

**(b) 四个产品的上架地区差异**（实测 164 个地区码）：

| 产品 | 上架地区数 | 未上架地区 |
|---|---:|---|
| Google Gemini | **161** | 白俄罗斯、**中国大陆**、俄罗斯 |
| ChatGPT | 158 | 白俄罗斯、**中国大陆**、**香港**、**澳门**、俄罗斯、委内瑞拉 |
| Claude | 155 | 上述 6 个 + 刚果（金）、缅甸、也门 |
| Grok AI | 151 | 上述 + 刚果（布）、伊拉克、黎巴嫩、利比亚、津巴布韦 等 |

这本身就是有价值的内容：**Gemini 是四家里唯一在港澳 App Store 上架的**，ChatGPT / Claude / Grok 都没有。

⚠️ 合规提示：`itunes.apple.com/robots.txt` 里有 `Disallow: /*/lookup?`。虽然我们的请求路径是根级 `/lookup?`（模式匹配存疑），但建议**把它当作低频用途**——地区币种映射和上架情况变化极慢，每月跑一次即可，不要放进每日爬虫。日常价格抓取只走 §3.1 的商店页，那个路径在 `apps.apple.com/robots.txt` 里是明确允许的（并且被写进了官方 sitemap）。

### 3.3 Google Play — ❌ 只有价格区间，不可用

实测 `play.google.com/store/apps/details?id=com.openai.chatgpt&gl={国家}`，页面里内购信息只有一个**区间字符串**：

| 地区 | Google Play 返回 |
|---|---|
| US | `$8.00 - $200.00 per item` |
| TR | `TRY 249.99 - TRY 9,999.99 per item` |
| IN | `₹399.00 - ₹19,900.00 per item` |

只有最低价和最高价，**没有单个 SKU 的价格**。做不了"ChatGPT Plus 在各地区多少钱"。

结论：Play 可以作为一个"最低档位价格"的旁证（比如 US 的 `$8.00` 对得上 ChatGPT Go），但撑不起产品。**建议 MVP 直接放弃。**

### 3.4 官网 Web 价格 — ❌ 基本不可行

从德国 VPS 实测三个官方定价页：

| 地址 | 结果 |
|---|---|
| `openai.com/chatgpt/pricing/` | **HTTP 403**（Cloudflare 机器人拦截，机房 IP 被拒） |
| `anthropic.com/pricing` | HTTP 301 重定向 |
| `one.google.com/about/google-ai-plans/` | HTTP 200，但未登录状态下只返回 USD，不做地区定价 |

而且即便能抓，Web 价格的地区差异也远小于 App Store：Anthropic 的 Web 端基本是全球统一 USD（2026 年 7 月起才为印度单独上了 INR 定价）。**真正的地区价格差异集中在 App Store 内购，这恰好是唯一好抓的那个。** 这一点对项目是利好。

### 3.5 汇率 — ✅ 两个免费无密钥源

| 源 | 货币数 | 密钥 | 实测 |
|---|---:|---|---|
| `open.er-api.com/v6/latest/USD` | 166 | 不需要 | ✅ 每日更新，含 NGN/COP/EGP/PHP/KZT/VND 等冷门币 |
| `cdn.jsdelivr.net/npm/@fawazahmed0/currency-api` | 341 | 不需要 | ✅ 走 jsDelivr CDN，可作备份源 |

两者当天 USD/CNY 分别为 6.737 和 6.722，差 0.2%——属于不同中间价源的正常差异。

⚠️ 不建议用 ECB（frankfurter.app）：只覆盖约 30 种主要货币，NGN / COP / EGP / PKR / TZS 这些全都没有，而这些恰恰是价格差异最大的地区。竞品 geopriced.com 用的就是 ECB，这可能就是它只做 46 个国家的原因。

**建议**：主源 `open.er-api.com`，备源 jsDelivr，每日拉一次并**落库存档**——历史价格必须配当时的汇率才有意义，不能用今天的汇率去换算上个月的价格。

---

## 4. 实测数据

### 4.1 ChatGPT Plus 月付 · 全球 158 个 App Store 地区

（2026-08-26 抓取，USD/CNY = 6.7370）

**最便宜 10 个：**

| # | 地区 | 币种 | 本地价 | ≈USD | ≈CNY |
|---:|---|---|---:|---:|---:|
| 1 | 菲律宾 PH | PHP | ₱ 999.00 | 16.20 | 109 |
| 2 | 巴基斯坦 PK | PKR | Rs 4,900.00 | 17.66 | 119 |
| 3 | 加拿大 CA | CAD | $24.99 | 18.06 | 122 |
| 4 | 日本 JP | JPY | ¥3,000 | 18.84 | 127 |
| 5 | 坦桑尼亚 TZ | TZS | 49,900.00 TZS | 19.05 | 128 |
| 6 | 越南 VN | VND | 499.000đ | 19.15 | 129 |
| 7 | 卡塔尔 QA | QAR | QAR 69.99 | 19.23 | 130 |
| 8 | 巴西 BR | BRL | R$ 99,90 | 19.38 | 131 |
| 9 | 印尼 ID | IDR | Rp 349ribu | 19.70 | 133 |
| 10 | 埃及 EG | EGP | EGP 999.99 | 19.83 | 134 |

**最贵 6 个：**

| # | 地区 | 币种 | 本地价 | ≈USD | ≈CNY |
|---:|---|---|---:|---:|---:|
| 153 | 斯洛伐克 SK | EUR | 22,99 € | 26.83 | 181 |
| 154 | 波兰 PL | PLN | 99,99 zł | 27.12 | 183 |
| 155 | 英国 GB | GBP | £19.99 | 27.27 | 184 |
| 156 | 丹麦 DK | DKK | 179,00 kr | 27.93 | 188 |
| 157 | 匈牙利 HU | HUF | HUF8,990.00 | 29.02 | 196 |
| 158 | 哥伦比亚 CO | COP | $ 99.900,00 | 32.62 | 220 |

**最低比最高便宜 50%**（$16.20 vs $32.62）。158 个地区一共只有 **42 个不同的美元价位**。

### 4.2 一个值得做的发现：网上流传的"土耳其最便宜"已经过时

| 地区 | 排名 | ≈USD |
|---|---:|---:|
| 菲律宾 PH | **#1** | 16.20 |
| 日本 JP | #4 | 18.84 |
| 美国 US | #11（与 85 个地区同价） | 19.99 |
| **土耳其 TR** | **#97** | **20.79** |
| 印度 IN | #98 | 20.95 |
| 尼日利亚 NG | #118 | 23.32 |
| 德国 DE | #136 | 26.83 |
| 英国 GB | #155 | 27.27 |

⚠️ **排名口径必须说清楚，否则会得出错的结论。** 158 个地区只有 **42 个不同价位**——光 `$19.99` 就有 **86 个地区**（美元区共用一个"世界其余地区"基准价）。所以排名必须用竞赛排名（比它严格便宜的地区数 + 1），并列同名次；按排序后的数组下标取名次，在这一大坨并列里完全是任意的。

这一点在开发前端时才被发现：同一个美国，Python 脚本排出第 90，Go 导出的数据排出第 80，**两个都没意义**。真实答案是第 11 位，与另外 85 个地区同价。本文上面表格已按竞赛排名更正。

顺带一提，这反而是个更有意思的结论：**美国价就是全球美元基准价，一大半地区跟它一样**，真正贵的是欧洲、英国、北欧那批离群值。

搜索结果里大量 2026 年的文章还在写"土耳其 ₺499.99 最便宜"，实测当天土耳其已经是 **₺999.99，排名 97/158**（土耳其无并列，这个名次两种算法一致）。尼日利亚、印度这些被反复推荐的"低价区"同样早就不便宜了。

**这就是这个项目最实在的价值主张：这类信息衰减极快，静态文章一写完就开始过期，只有每日自动抓取才有意义。**

### 4.3 相同货币 ≠ 相同价格（重要架构结论）

158 个地区里有 97 个用 USD、23 个用 EUR。直觉上可以按币种去重，只抓 40 个代表地区——**实测证明这是错的**：

| 币种组 | 地区 | ChatGPT Plus | ChatGPT Go |
|---|---|---:|---:|
| USD | 美国 US | $19.99 | $8.00 |
| USD | 斯里兰卡 LK | $19.99 | $5.00 |
| USD | 尼泊尔 NP | $19.99 | $5.69 |
| USD | 阿根廷 AR | $19.99 | $5.99 |
| USD | **乌克兰 UA** | **$22.99** | $6.99 |
| USD | **肯尼亚 KE** | **$22.99** | $5.99 |
| EUR | 德国 DE / 法国 FR / 葡萄牙 PT | 22,99 € | 7,99 € |
| EUR | **保加利亚 BG** | **19,99 €** | **6,49 €** |
| EUR | 塞尔维亚 RS | 22,99 € | **5,99 €** |

同样是美元区，乌克兰和肯尼亚贵 15%；同样是欧元区，保加利亚便宜 13%。ChatGPT Go 的分化更夸张，从 $5.00 到 $8.00。

**结论：必须逐个抓完 158 个地区，不能按币种抽样。** 反过来说，这也是内容护城河——竞品做 20–46 个国家，必然漏掉保加利亚、斯里兰卡这种"同币种洼地"。

### 4.4 四个产品的主力档位横向对比（全量实测）

| 产品 · 档位 | 有价格的地区数 | 最低 | 最高 | 价差 | 美国 |
|---|---:|---|---|---:|---|
| ChatGPT Plus | 158 | 🇵🇭 PH $16.20 | 🇨🇴 CO $32.62 | 50% | $19.99（#11，86 地区同价） |
| ChatGPT Go | 158 | 🇮🇳 IN $4.18 | 🇳🇴 NO $10.62 | 61% | $8.00（#112） |
| Claude Pro 月付 | 155 | 🇵🇰 PK $17.66 | 🇨🇴 CO $32.62 | 46% | $20.00（#71） |
| Claude Max 5x 月付 | 155 | 🇮🇩 ID $112.84 | 🇨🇴 CO $195.86 | 42% | $124.99（#68） |
| Google AI Pro (5TB) | 157 | 🇹🇷 TR $11.22 | 🇭🇺 HU $28.37 | 60% | $19.99（#95） |
| Google AI Plus (400GB) | 155 | 🇹🇷 TR $4.16 | 🇲🇪 ME $58.34 ⚠️ | — | $4.99（#70） |
| Google AI Ultra (30TB) | **87** ⚠️ | 🇧🇭 BH $149.99 | 🇲🇪 ME $291.73 | 49% | $199.99（#54） |
| SuperGrok | 148 | 🇹🇷 TR $27.02 | 🇳🇴 NO $42.82 | 37% | $30.00（#78） |
| SuperGrok Lite | 149 | 🇯🇵 JP $9.42 | 🇨🇴 CO $16.29 | 42% | $10.00（#73） |
| SuperGrok Heavy | **128** ⚠️ | 🇵🇭 PH $243.01 | 🇳🇴 NO $428.21 | 43% | $300.00（#61） |

几个值得注意的点：

- **"最便宜的地区"每个产品都不一样。** ChatGPT 是菲律宾，Claude 是巴基斯坦，Gemini 和 SuperGrok 是土耳其，SuperGrok Lite 是日本。**没有一个"万能低价区"**——这正好是做成对比工具而不是写一篇文章的理由。
- **土耳其对 Gemini 便宜得离谱**（$11.22，比美国低 44%），但对 ChatGPT 只排到 97/158。同一个地区在不同产品上的定价策略完全不同。
- ⚠️ 标记的两行说明了 §5.2 的截断问题：**Google AI Ultra 只在 87/158 个地区能查到，SuperGrok Heavy 只在 128/158**。不是这些地区不卖，而是没挤进 Apple 只展示前 10 条的名单。
- ⚠️ 黑山 ME 的 Google AI Plus $58.34 是**年付价被当成月付**——该地区的月付 SKU 没进前 10，只剩年付。这类脏数据必须靠 §5.3 的白名单和价格区间约束挡住，不能直接上线。

---

## 5. 工程难点

### 5.1 价格字符串解析：单个 SKU 就有 78 种格式

只统计 "ChatGPT Plus" 这一个 SKU 在 158 个地区的显示字符串，就有 **78 种不同的格式模板**。踩到的坑：

| 坑 | 实例 | 说明 |
|---|---|---|
| **同符号相反约定** | US `$19.99` vs CO `$ 99.900,00` | 都是 `$`。哥伦比亚是点作千分位、逗号作小数点 |
| 智利式无小数 | CL `$999.999` | 点是千分位，整个数没有小数部分 |
| **本地化数量词** | ID `Rp 349ribu`、`Rp 1,999juta` | 印尼语 ribu=千、juta=百万。不处理会把 349,000 解析成 349 |
| **数量词里的逗号是小数点** | ID `Rp 1,999juta` = 1,999,000 | 同一个逗号，在 `1,999juta` 里是小数点，在 `4,900.00` 里是千分位 |
| 空格千分位 | CZ `9 999,99 Kč` | 而且是 NBSP（`\xa0`），不是普通空格 |
| 货币码位置不定 | `USD 19.99` / `22,99 USD` / `19,99 $US` | 前缀、后缀、法语变体都有 |
| 无空格前缀 | HU `HUF8,990.00` | 货币码和数字之间没有分隔 |
| 零小数币种 | JP `¥3,000`、KR `￦99,999` | 逗号是千分位，不是小数点 |

**解决方案（已验证全部主力档位通过）：**

1. **绝不从货币符号推断货币**——必须用 §3.2 拿到的「地区 → ISO 币种」映射作为基础事实。
2. **零小数币种用 ISO 4217 的 exponent 表，不要凭直觉列。** HUF、COP、TZS 看起来像零小数币种，但 Apple 就是按两位小数显示（`HUF8,990.00`、`$ 99.900,00`）；真正 exponent=0 的是 JPY / KRW / VND / CLP / ISK / PYG / XOF / XAF 等。
3. 分隔符判定规则：最后一个分隔符后**恰好 2 位数字**且该分隔符**在串中只出现一次**且**该币种有小数位** → 它是小数点；否则所有分隔符都是千分位。**但只要出现了 ribu/juta 这类数量词，分隔符一律按小数点处理。**
4. **上线合理性校验**：折算成 USD 后超出该档位预期区间的直接标记异常、不发布。

⚠️ 第 4 条不是可选项。实测过程中，**校验规则一共抓出了两轮解析 bug**：

- 第一轮：凭直觉把 HUF/COP/TZS 当成零小数币种 → 匈牙利、哥伦比亚、坦桑尼亚三地价格算大 100 倍；
- 第二轮：修好第一轮之后，印尼的 `Rp 1,999juta` 被算成 1,999 百万 → Claude Max 5x 在印尼显示成 **$112,844.99**。

两次都不是靠肉眼看出来的，是靠区间校验报出来的。**没有这条规则，站点会静默地发布错得离谱的价格。**

### 5.2 Apple 只展示前 10 个内购项目

商店页的内购列表按该地区热度排序，**最多 10 条**。全量实测下来，每个 SKU 能覆盖到多少地区差别巨大：

| 产品 | SKU | 覆盖地区 / 上架地区 |
|---|---|---:|
| ChatGPT | Plus / Go / Pro 5x / Pro 20x | **158 / 158** ✅ |
| Claude | Pro 月付 / Max 5x / Max 20x / Pro 年付 | **155 / 155** ✅ |
| Claude | Usage Credits | 153 / 155 |
| Gemini | Google AI Pro (5 TB) | 157 / 158 ✅ |
| Gemini | Google AI Plus (400 GB) | 155 / 158 ✅ |
| Gemini | **Google AI Ultra (30 TB)** | **87 / 158** ⚠️ |
| Gemini | Google AI Plus (2 TB) | 51 / 158 ⚠️ |
| Grok | SuperGrok / Lite / Plus | 148–149 / 150 ✅ |
| Grok | **SuperGrok Heavy** | **128 / 150** ⚠️ |

影响：

- **ChatGPT 和 Claude 的主力档位 100% 无损**，这两个产品可以放心做。
- **Gemini 的 AI Ultra 和 Grok 的 Heavy 有大面积缺口**，因为高价档位买的人少、挤不进前 10。
- 某个档位在某地区"查不到"**不等于该地区没有这个档位**。前端必须严格区分「该地区未上架」和「未进入前 10、未能获取」，绝不能显示成"不可用"——那是错误信息。
- Gemini 还有一批**只在个别地区出现的 SKU**：`Google AI Pro (10 TB)` 和 `Google AI Ultra (20 TB)` 各只在 2 个地区出现。说明 Google 在不同地区卖的存储档位本身就不一样，不只是价格不同。

### 5.3 拿不到订阅周期，必须人工维护 SKU 表

这是最需要人工介入的地方。Apple 只给「名称 + 价格」，**不给订阅周期**。实测的问题：

```
ChatGPT 美区：
  ChatGPT Plus      $19.99   ← 月付
  ChatGPT Plus      $200.00  ← 年付，但名字一模一样
  ChatGPT Pro 20x   $200.00  ← 和上面同价不同物

Gemini 美区：
  Google AI Pro (5 TB)  $19.99  ← 同一个名字出现 5 次
  Google AI Pro (5 TB)  $19.99     （不同 SKU，月付/年付/促销价）
  ...
```

Claude 相对规范（`Claude Pro - Monthly` / `Claude Pro - Annual` 名字里自带周期），但 OpenAI 和 Google 完全不规范。**Google 是四家里最麻烦的**：同名 SKU 重复出现、存储档位按地区变化（`AI Ultra (30 TB)` vs `(20 TB)`）、法语地区还改名。土耳其区甚至出现两条都叫 `Google AI Pro (5 TB)` 但价格分别是 ₺869,99 和 ₺539,99 的记录。

**难度排序：ChatGPT ≈ Claude（容易）＜ Grok ＜ Gemini（明显最难）。** 如果要砍范围，先砍 Gemini 的细分存储档位，只保留 AI Pro / AI Plus / AI Ultra 三个主档。

**解决方案**：维护一张人工 SKU 白名单，形如

```
产品      展示名                 归一化档位      周期    价格区间约束
ChatGPT   ChatGPT Plus          plus           月付    5-60 USD
ChatGPT   ChatGPT Plus          plus           年付    150-400 USD
ChatGPT   ChatGPT Go            go             月付    2-15 USD
Claude    Claude Pro - Monthly  pro            月付    10-40 USD
```

靠「名称 + 价格区间」两个条件联合判定周期。规模约 **40 条**，产品改版时才需要更新。**这是全项目唯一的持续人工成本，可接受。**

⚠️ 但要诚实说明：这条规则是启发式的。如果 OpenAI 哪天把 Plus 年付降到 $60 以下，规则就会误判。所以需要**变更告警**：任何 SKU 价格相对上一次快照变动超过阈值（比如 20%），先进人工审核队列，不直接发布。

### 5.4 名称基本不本地化 —— 这是个好消息（但有例外）

全量实测 158 个地区，内购项目名称**绝大多数完全一致**：`ChatGPT Plus` 在日区、土区、埃及区都叫 `ChatGPT Plus`，`Claude Pro - Monthly` 也一样。这意味着**跨地区匹配可以直接用名称精确匹配**，不需要模糊匹配或翻译对齐——原本预计最大的风险消掉了。

⚠️ 但有例外，而且只出现在 Google 身上：法语地区（`fr`、`ci`）的存储档位叫 `100 Go` / `200 Go`（Go = Gigaoctet），其他地区叫 `100 GB` / `200 GB`。

所以规则是：**以名称精确匹配为主，但 SKU 白名单要支持一个 SKU 挂多个别名**。目前只需要为 Google 的存储档位配几条别名，成本很低——但不能假设"名称永远全球统一"。

### 5.5 中国 IP 抓不了

从本机（国内 IP）访问 `https://apps.apple.com/us/app/id6448311069`，**无论指定哪个地区码，一律 302 跳到 `https://apps.apple.com/cn/iphone/today`**。只有 `/cn/` 路径能正常解析。

从德国 VPS 访问，`/us/`、`/jp/`、`/tr/`、`/in/` 全部正常返回 200。

结论：

- 爬虫**必须**跑在海外机器上——现有 netcup 德国 VPS 正合适。
- **本地开发调试需要走代理**，否则拿到的永远是中国区 Today 页。这一点会浪费时间，提前知道能省事。

---

## 6. 技术架构

### 6.1 目录结构（沿用本仓库既有模式）

现有仓库已经有两套「`web/` + `api/` + `deploy/`」的站点（`blog/`+`server/`、`tutu/`），新站按同样模式加第三个：

```
aiprice/
  api/       Go：爬虫 + 解析 + REST 接口
  web/       Astro 静态站（SEO 需要预渲染）
  deploy/    部署脚本、systemd service + timer、OpenResty 反代配置
```

**前端选 Astro 而不是 Vite+React SPA**：这个站的流量全靠搜索引擎——「ChatGPT 哪个区最便宜」「Claude Pro 价格 对比」这类长尾词。4 个产品 × 158 个地区 + 各产品排行页 ≈ 600+ 个需要被索引的页面，必须静态预渲染。`tutu/` 那套 SPA 模式不适合。这里跟 `blog/` 保持一致（Astro + Tailwind），构建产物同样丢给 OpenResty 做静态托管。

### 6.2 数据流

```text
每日 systemd timer 触发
    ↓
Go 爬虫：4 App × 158 地区，节流 1.2s，全程约 42 分钟
    ↓
解析 serialized-server-data → (地区, SKU 名, 格式化价格串)
    ↓
按「地区 → 币种」映射解析成 数值 + ISO 币种
    ↓
拉当日汇率并落库（历史价格必须配历史汇率）
    ↓
按 SKU 白名单归一化档位和周期
    ↓
合理性校验 + 变价告警（超阈值进人工队列，不直接发布）
    ↓
写入 price_snapshots（每日一条，永不覆盖）
    ↓
触发 Astro 构建 → 输出静态站 → 部署
```

### 6.3 数据模型

`storefronts` — 地区码、中英文名、ISO 币种、旗帜 emoji。约 158 行，月更。

`products` — 产品（chatgpt / claude / gemini / grok）、App Store ID、Bundle ID、厂商。

`skus` — **人工维护的白名单**：产品、Apple 展示名、归一化档位、周期、价格区间约束。约 40 行。

`price_snapshots` — 核心表，**只追加不更新**：
- `date`、`product_id`、`sku_id`、`storefront`
- `raw_string`：Apple 原始格式化字符串，**务必保留**，解析出错时唯一的排查依据
- `amount_minor`、`currency`：最小货币单位整数，不用浮点
- `parse_status`：ok / 异常 / 待人工审核
- 唯一索引 `(date, product_id, sku_id, storefront)`

`fx_rates` — `date`、`base`、`currency`、`rate`、`source`。每日全量存档。

`availability` — 产品 × 地区 × 是否上架，月更。

### 6.4 关键工程要求

- 抓取节流 ≥1 秒，429 时指数退避重试；单次全量失败率超过 5% 就中止并告警，**不要用残缺数据覆盖昨天的好数据**。
- `raw_string` 永久保留。78 种格式模板意味着解析器一定还会遇到没见过的第 79 种。
- 快照只追加。历史变价记录是本项目相对竞品的核心差异，不能被覆盖掉。
- 汇率随快照一起存档，页面上明确标注「汇率取自 X，更新于 Y」。
- 页面标注**精确的抓取时间戳**。竞品普遍写「9 天前验证」「2026 年 7 月更新」，这是可以正面碾压的点。
- 前端严格区分三种状态：**有价格 / 该地区未上架 / 未进入前 10 未能获取**。

### 6.5 成本

| 项 | 成本 |
|---|---|
| 服务器 | ¥0，复用现有 netcup VPS（4 核 8G，日增负载 42 分钟单线程） |
| 数据库 | ¥0，复用现有 MySQL |
| CDN / 证书 | ¥0，复用现有 Cloudflare |
| 数据源 | ¥0，Apple 商店页和两个汇率 API 都免费无密钥 |
| 域名 | 约 ¥70 / 年 |

存储量级：四个产品合计约 40 个 SKU × 158 地区 × 365 天 ≈ **230 万行/年**，MySQL 完全扛得住，必要时按年分区。

---

## 7. 竞品与差异化

这个赛道**已经有人做了**，必须正视：

| 站点 | 覆盖 | 更新方式 | 变现 |
|---|---|---|---|
| appstoreprice.org | ChatGPT 页 32 个地区 | 未标注 | 站内礼品卡商城 |
| geopriced.com | 46 个国家，多品类 | 「9 天前验证」，ECB 汇率 | NordVPN 联盟 |
| aisubscriptioncomparison.com | 20+ 国家，30+ 模型 | 「2026-04-17 验证」 | 联盟佣金 |
| opentherank / aizolo / 各类博客 | 少量国家 | 静态文章 | 联盟 / 广告 |

**共同弱点，且都是可攻击的：**

1. **覆盖太浅。** 最多 46 个国家。而 §4.3 证明了同币种地区价格并不相同——保加利亚（欧元区最便宜）、斯里兰卡（美元区 Go 最便宜）这类洼地，抽样式覆盖必然漏掉。
2. **数据陈旧。** 「4 月验证」「7 月更新」。而 §4.2 显示土耳其价格已经翻倍、排名从第 1 掉到第 97，这些站还在推荐土耳其。
3. **没有历史。** 没有一家做变价追踪。而这恰恰是唯一能形成复访和积累的数据资产——爬一天没价值，爬一年就是别人补不回来的东西。
4. **靠人工。** 「verified」的措辞说明是人工核对的，这决定了它们不可能同时做到广覆盖和高频更新。

**差异化定位：**

> 158 个 App Store 地区 × 4 个 AI 产品 × 每日自动抓取 × 完整历史变价曲线，每条价格标注精确抓取时间和原始字符串。

第 3 点（历史）是最难被抄的，因为它需要时间。**所以即便产品形态还没想清楚，爬虫也应该尽早跑起来攒数据。**

⚠️ 需要诚实评估的未知数：**搜索流量到底有多大没有验证过。** 上线前建议先用关键词工具确认「chatgpt 价格 对比」「claude 哪个区便宜」这类词的实际搜索量，别做完才发现是个没人搜的需求。这是本次调研**没有覆盖**的部分。

---

## 8. 合规与法律

这一节是本项目**风险最集中**的地方，比技术难得多。

### 8.1 抓取本身：基本合规

`apps.apple.com/robots.txt`：

```
Disallow: /WebObjects/*
Disallow: /api/*
Disallow: /v1/*
Disallow: */search?*
Sitemap: https://apps.apple.com/sitemaps_apps_index_app_1.xml
```

我们抓的 `/{地区}/app/id{ID}` **不在禁止列表内，而且被官方 sitemap 主动收录**。加上节流、正常 UA、不绕鉴权，属于常规抓取。

对比之下，`amp-api.apps.apple.com/v1/...`（App Store 网页版内部 API，需要从页面里抠 Bearer token）落在 `Disallow: /v1/*` 里，而且要绕鉴权。**明确建议不要用**，虽然它能返回更结构化的数据。§3.1 的商店页方案已经够了。

### 8.2 真正的风险：不要做成"跨区套利教程"

竞品的变现路径高度一致：**VPN 联盟 + 礼品卡商城**，卖的就是"切到菲律宾区省 50%"。这条路的问题：

- Apple 的 Media Services 条款要求，切换 Apple 账号国家/地区**必须提供该国的有效支付方式和账单地址**。中国用户为了省钱去切菲律宾区，通常做不到——只能靠代充或买礼品卡绕开，这是违反条款的，**账号被锁的后果由用户承担**。
- OpenAI / Anthropic 的服务条款同样限制规避地区定价。
- 站点如果提供**操作教程 + 礼品卡销售**，就从"信息展示"变成了"提供规避手段并从中获利"，法律定位完全不同。

**建议的定位：**

- ✅ 展示客观价格数据、换算、排名、历史曲线——这是事实信息，合理使用。
- ✅ 明确说明"价格差异源于各厂商的购买力平价定价策略"。
- ✅ 在页面上说明切换地区的**条件和风险**（需要当地支付方式、违反条款可能导致账号问题）——这是保护用户，也是给自己划清界限。
- ❌ 不写"手把手教你切区"的操作教程。
- ❌ 不卖礼品卡、不接 VPN 联盟。这是竞品的主要收入来源，也是它们最大的法律敞口。

这会让变现变难。**但这是需要你自己拍板的取舍，我的建议是先不考虑变现**——当作技术项目和流量入口做，跟现有博客互相导流。真要变现，优先考虑四家厂商自己的推荐计划这类正规渠道。

### 8.3 商标与免责

- 「ChatGPT」「Claude」「Gemini」「Grok」都是注册商标。用于**对比和指代**属于指示性合理使用，但：
  - 不要用它们的 Logo 做站点品牌元素；
  - 页脚必须写明"本站与 OpenAI / Anthropic / Google / xAI 均无关联"；
  - 域名里不要嵌入这些商标（`chatgptprice.com` 这种会有麻烦）。
- 必须写明"价格数据来自 Apple App Store 公开页面，仅供参考，以各平台实际结算为准"。

### 8.4 国内相关

- 站点部署在德国 VPS + Cloudflare，**不要用国内注册的域名、不要备案**。
- ChatGPT / Claude / Grok 在中国大陆和港澳 App Store 均未上架（§3.2），做中文站讨论这些服务的价格本身就有敏感性。
- 再次强调 §8.2：**不提供任何"如何访问/如何切区"的教程内容**，只做价格数据。这既是法律考虑，也是这类站点在国内环境下能否长期存在的关键。

---

## 9. 分阶段计划

### 阶段 0：验证（✅ 本次调研已完成）

数据源、抓取稳定性、解析可行性、汇率源全部实测通过。**没有需要继续验证才能动手的技术问题。**

唯一建议补做的验证：§7 提到的**关键词搜索量**。

### 阶段 1：先让爬虫跑起来（建议立刻做，1–2 天）

产品形态可以慢慢想，但**历史数据是攒出来的，早一天开始就多一天资产**。

- Go 爬虫 + 解析器 + `price_snapshots` 表；
- systemd timer 每日跑一次；
- 不做前端，先攒数据。

### 阶段 2：MVP 站点（约 1 周）

- SKU 白名单（约 40 条，先做 ChatGPT + Claude，Gemini 只保留三个主档）；
- 子域名 `aiprice.monkeykgai.com` 接入（§10.2，约 15 分钟）；
- Astro 静态站 + i18n：产品排行页（4 个）+ 地区详情页（158 个）× 中英双语 ≈ 1300 页；
- `hreflang` + 分语言 sitemap + Cloudflare 缓存规则；
- 每条价格标注抓取时间、原始字符串、所用汇率；
- 免责声明和商标声明 + §10.1 的切区风险提示。

### 阶段 3：差异化功能（攒够 1–2 个月数据后）

- 历史价格曲线 + 变价时间线（**核心壁垒**）；
- 变价 RSS / 邮件订阅；
- 「涨价榜」「降价榜」这类天然带传播性的页面；
- 扩品：Perplexity、GitHub Copilot、Cursor、Midjourney 等。

### 阶段 4：再谈变现

在 §8.2 的边界内考虑。**建议前三个阶段完全不碰变现。**

---

## 10. 已决策（2026-08-26）

| # | 决策 | 结论 |
|---|---|---|
| 1 | 定位与变现 | **保守路线**：不卖礼品卡、不接 VPN 联盟 |
| 2 | 域名 | **挂现有域名的子域**，新建子域名 + 配 Cloudflare |
| 3 | 语言 | **中英双语都做** |
| 4 | 范围 | **先只做四个产品** |

### 10.1 定位：保守路线（已定）

明确的禁止清单，写进项目规范：

- ❌ 不销售任何礼品卡、点卡、代金券
- ❌ 不接 VPN / 机场 / 代理类联盟营销
- ❌ 不写"如何切换 App Store 地区"的操作教程
- ✅ 页面必须有风险提示：切区需要当地支付方式、违反 Apple Media Services 条款可能导致账号问题
- ✅ 前三个阶段完全不考虑变现

### 10.2 域名与部署：子域名（实测后发现比预想简单）

上服务器核对了一遍，有两个和仓库文档不一致的现状，都是好消息：

1. **子域名模式服务器上已经在用了。** `conf.d/` 里已有 `komari.monkeykgai.com.conf` 和 `umami.monkeykgai.com.conf` 两个独立子域名站点。本仓库 README 只记录了 blog 和 tutu 的**路径模式**（`monkeykgai.com/tutu/`），所以看代码会以为子域名是新路子——其实 1Panel 上是现成的。
2. **证书不用重新签。** 1Panel 里的 Cloudflare Origin CA 证书 SAN 是 `*.monkeykgai.com` + `monkeykgai.com`，**有效期到 2041-07-10**。Cloudflare 边缘证书同样是 `*.monkeykgai.com`（Google Trust Services 签发）。一级子域名两端都自动覆盖。

**接入步骤（约 15 分钟）：**

1. 起名：建议 **`aiprice.monkeykgai.com`**（`price.` 太泛，`aiprice.` 表意准且好记）
2. Cloudflare → DNS → 加 A 记录 `aiprice` → `159.195.18.74`，**开橙色云代理**
3. 1Panel → 网站 → 创建**静态网站**，域名填 `aiprice.monkeykgai.com`
4. 该站点 SSL → 导入现有 Cloudflare Origin 证书（直接复用 `/www/sites/monkeykgai.com/ssl/` 那一对）
5. Cloudflare SSL 模式保持 **Full (Strict)**，不用改
6. ufw 不用动（80/443 已经只放行 Cloudflare IP 段）
7. 新增 `aiprice/deploy/deploy-web.sh`，照抄 `deploy/deploy-blog.sh`，改 `WEB_ROOT`

**⚠️ 必须单独配 Cloudflare 缓存规则。** 价格页每天变，博客那套长缓存会把旧价格缓存住。给该子域名单独设 Cache Rule，HTML 建议 `s-maxage=3600` + `stale-while-revalidate`，静态资源才走长缓存。这是最容易忘、且忘了以后现象很隐蔽的一步（页面显示的价格和数据库里的对不上）。

**为什么子域名在这里是对的**（不只是省事）：SEO 上子域名确实拿不到主域权重，但 monkeykgai.com 是个人博客，可转移的权重本来就有限；换来的是**合规隔离**——独立的 robots、sitemap、统计、风险提示和缓存策略，将来价格站真出问题也不牵连主博客。结合 §8 的定位要求，这个隔离比那点权重值钱。

### 10.3 中英双语（已定）

- **URL 结构**：`/zh/...` 和 `/en/...`，根路径按 `Accept-Language` 跳转，默认落 `/en/`（英文搜索量更大）
- **必须配 `hreflang`**：`zh-Hans` / `en` / `x-default`。双语站不配 hreflang 会被判重复内容，白做
- **sitemap 按语言分文件**
- **翻译工作量比看上去小**：158 个地区名 + 40 种货币名可以直接用 **CLDR 数据自动生成两套**，不需要手翻。真正要人工写的只有页面文案和 SKU 展示名，几十条
- **页面量翻倍**：4 产品 × 158 地区 × 2 语言 + 排行页 ≈ **1300+ 页**。Astro 构建这个量级几十秒，没问题
- **⚠️ 两种语言必须共用同一份价格数据**，只在展示层分语言。绝不做两套数据，否则一定会出现中英文价格对不上

### 10.4 只做四个产品，但表结构必须可扩展

- `products` 表驱动，**不要把 chatgpt / claude / gemini / grok 硬编码进代码**。加一个产品 = 加一行记录 + 几条 SKU 白名单
- 这次实测已经抓到四个产品的全量快照（`/root/aiprice-probe/`），可以直接当第一批种子数据导入
- 将来扩 Perplexity / Copilot / Cursor / Midjourney 时，只需要查一下 App Store ID，抓取和解析逻辑完全复用

### 10.5 关于后续的代充值站：现在就要把边界划好

你提到后面规划做代充值站（比如代充 GPT），本次不做进去——这是对的。但有一点现在就得想清楚，因为它会**反过来影响价格站的架构**：

价格站选保守定位（决策 1）的核心逻辑是「只提供事实信息，不从跨区差价里获利」。**如果将来价格站给自己的代充站导流，这条边界就没了**——形式上只是把「拿联盟佣金赚差价」换成了「自己赚差价」，§8.2 分析的风险一个不少地回来，而且因为是自营，责任更直接。

所以建议现在就把两件事物理隔开：

- **不同域名**（不要做成 monkeykgai.com 下的两个子域），最好不同主体
- 价格站上**不出现任何指向代充服务的链接、按钮或广告位**
- 两边不共用数据库、不共用统计、不互相 canonical

这样两个站各自独立成立，哪边出问题都不连坐。

⚠️ 但要说明白：**如果你的规划本来就是让价格站给代充站引流，那决策 1 的"保守定位"实际上不成立**，应该现在重新评估定位，而不是等站建好、内容和外链都积累起来了再改——那时候改成本高得多。这个我不替你判断，但这两条路不能同时走。

---

## 11. 资料索引

### 本次实测的接口

- App Store 商店页：`https://apps.apple.com/{storefront}/app/id{appId}` — 解析 `<script id="serialized-server-data">`
- iTunes Lookup：`https://itunes.apple.com/lookup?id={appId}&country={cc}` — 币种与上架情况
- iTunes Search：`https://itunes.apple.com/search?term={q}&country=us&entity=software` — 查 App ID
- 汇率主源：<https://open.er-api.com/v6/latest/USD>
- 汇率备源：<https://cdn.jsdelivr.net/npm/@fawazahmed0/currency-api@latest/v1/currencies/usd.json>
- `https://apps.apple.com/robots.txt` / `https://itunes.apple.com/robots.txt`

### 参考与竞品

- [appstoreprice.org（参考对象）](https://appstoreprice.org/zh)
- [geopriced.com](https://geopriced.com/service/google-one)
- [aisubscriptioncomparison.com](https://aisubscriptioncomparison.com/pricing/global-price-map/)

### 合规

- [Apple：更改 Apple 账户国家或地区（需当地支付方式与账单地址）](https://support.apple.com/en-us/118283)
- [Anthropic 开始为印度做本地化定价（TechCrunch, 2026-07-13）](https://techcrunch.com/2026/07/13/anthropic-starts-localizing-claude-pricing-for-india-its-biggest-market-after-the-us/)
- ISO 4217 货币小数位（exponent）表 — 解析器必须依赖，见 §5.1
