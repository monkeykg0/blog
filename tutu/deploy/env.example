# 复制到服务器 /opt/media-api/env 并填好实际值(此文件不入库真实值)
LISTEN=127.0.0.1:8081
MEDIA_ROOT=/opt/media
# 前端页面内嵌的共享访问令牌,自己生成一串随机字符:openssl rand -hex 16
MEDIA_TOKEN=改成随机串
# 复用 blog 库(表 media_progress 自动创建),密码与 blog-stats 的一致
MYSQL_DSN=blog:改成密码@tcp(127.0.0.1:3306)/blog?parseTime=true
